namespace Sample;

// Trivial production code for the minimal scenario's build to compile.
public static class Greeter
{
    public static string Greet(string name) => $"Hello from the Fallout canary, {name}!";
}
