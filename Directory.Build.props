<Project>

  <!-- Scenarios float their Fallout package versions inline (2026.1.*-*), so central
       package management is deliberately OFF. Do not add a Directory.Packages.props. -->
  <PropertyGroup>
    <ManagePackageVersionsCentrally>false</ManagePackageVersionsCentrally>
  </PropertyGroup>

</Project>
